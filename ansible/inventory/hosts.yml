all:
  children:
    dev:
      hosts:
        localhost:
          ansible_connection: local
    prod:
      hosts:
        vote_backend:
          ansible_connection: ssh
          ansible_user: vote
          ansible_become_password: "{{ vault_vote_become_password }}"
