# Managed by Ansible — roles/db-maintenance
# Do not edit by hand; changes will be overwritten on next deploy.

PGHOST=localhost
PGDATABASE="{{ vote_backend_db_name }}"
PGUSER="{{ vote_backend_db_maintenance_user }}"
PGPASSWORD="{{ vote_backend_db_maintenance_password }}"
DB_MAINTENANCE_CONTAINER="{{ vote_backend_db_container_name }}"
DB_MAINTENANCE_RETENTION_DAYS="{{ vote_backend_rejected_questions_retention_days }}"
